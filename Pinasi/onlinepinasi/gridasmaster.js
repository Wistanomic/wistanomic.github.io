window.addEventListener('load', function() {var queryString = location.search.substring(1);l=JSON.parse(sessionStorage.getItem('datajson')); var k=[];const pageSize = glimit||10;var curPage = 1; if(pagi.checked == true){pagination();} else{$('#grid').html(""); grid(l,"","reverse");}  });function pagination(){var k=[];const pageSize = glimit||10;var curPage = 1; $('#grid').html("");   k=  l.filter((row, index) => {let start = (curPage-1)*pageSize;let end =curPage*pageSize;if(index >= start && index < end) return true;});grid(k,"reverse");nextPage.addEventListener("click", function () { if((curPage * pageSize) < l.length) curPage++;k=  l.filter((row, index) => {let start = (curPage-1)*pageSize;let end =curPage*pageSize;if(index >= start && index < end) return true;});$('#grid').html(""); grid(k,"reverse");},false);  prevPage.addEventListener("click", function () { if(curPage > 1) curPage--;k=  l.filter((row, index) => {let start = (curPage-1)*pageSize;let end =curPage*pageSize;if(index >= start && index < end) return true;});$('#grid').html(""); grid(k,"reverse");},false); } function src() {$('#grid').html(""); grid(l,"","reverse");}
function mgrid(nm) {$('#grid').html(""); grid(l,nm,"reverse");}
function finm(nm) {$('#grid').html(""); grid(l,nm,"");}